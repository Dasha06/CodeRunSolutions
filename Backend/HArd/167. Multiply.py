import sys


def main():
    n, m, k = map(int, input())
    a = list(map(int, input()))
    """идея моя в том, чтобы в начале провести цикл, что определит множители числа m, 
    а потом провести второй цикл с вложенным циклом, где будет проходить уже вычисление множителей из массива a"""

if __name__ == '__main__':
    main()
